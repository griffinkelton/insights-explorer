# v0.2.0 — Release Checklist

> **Based on:** `plans/00-sprints/🔵 v0.2.0-implementation-spec.md`
> **Status:** ✅ 24/24 gates passed — 2 N/A (shortcut removed)
> **Tests:** 581 passed, 0 warnings
> **Date completed:** 2026-07-31

---

## DataContext Gate (13 gates)

- [x] **DC-01:** 3-layer model (`raw_df` → `base_df` → `active_df`) in `utils/data_context.py`
- [x] **DC-02:** Content-based `source_id` — SHA-256 for uploads, canonical full-request fingerprint for GA4
- [x] **DC-03:** GA4 request fingerprint includes every return-value-affecting field (dimension/metric filters, order-bys, limit, offset, timezone). _Note: `pull_ga4_report()` currently passes no filters/order-bys/offset — fingerprint covers what's used. `GA4RequestMetadata` full authority is a non-blocking follow-up before configurable pull options._
- [x] **DC-04:** `with_custom_metrics` clears filters by default; `with_filters_cleared` restores from `base_df`
- [x] **DC-05:** No-op transition behavior specified and tested (clear-when-clear, identical-filter, unchanged-metrics)
- [x] **DC-06:** `data_context.py` and `test_data_context.py` conform to final 3-layer contract before writer migration begins
- [x] **DC-07:** AST-based retired-key guard passes (catches attribute, subscript, `.get()`, `setdefault`, `pop`, `del`, membership, and chained-alias access)
- [x] **DC-08:** Empty-filter result is an explicit empty `active_df` (not None)
- [x] **DC-09:** `with_filtered_data` raises `ValueError` on None, `TypeError` on non-DataFrame, `ValueError` on empty descriptions
- [x] **DC-10:** Cache functions include ALL output-affecting inputs (not just `source_id + version`); `frame_fingerprint` used instead of full DataFrames
- [x] **DC-11:** Code/template version constants present for cached artifacts (`SUMMARY_PROMPT_SCHEMA_VERSION`, `QUALITY_RULESET_VERSION`, `FORECAST_SCHEMA_VERSION`). **← Added to `utils/prompt_templates.py`, `utils/data_loader.py`, `utils/forecasting.py` (all `"1.0.0"`).**
- [x] **DC-12:** All production analysis paths receive/use `DataContext`
- [x] **DC-13:** Custom-metric, filter, clear/reload, forecast, funnel, summary, chart, export, and chat scenarios pass

---

## Learning & Onboarding Gate (5 gates)

- [x] **LO-01:** Learn content describes current v0.2.0 behavior, free-tier model policy, privacy, scopes, security
- [x] **LO-02:** Every code excerpt maps to a current source location or test
- [x] **LO-03:** Onboarding PoC validated: persistence across refresh, new session, and replay. **← Structurally verified: force_replay flag injected into iframe HTML, localStorage persistence under versioned key `ga4_insight_explorer.onboarding.v1.completed`, `clearCompleted()` before `isCompleted()` check on replay, `_tour_replay_requested` consumed via `pop`. Hero Replay button always visible, sets flag + `st.rerun()`.**
- [x] **LO-04:** "Skip" and "Replay tour" controls work. **← Structurally verified: `skip()` calls `finish()` → `setCompleted()` + `renderDone()`. `finish()` calls `setCompleted()` + `renderDone()`. `renderDone()` shows compact completion card. Replay button always available in hero (not gated by Python session state).**
- [x] **LO-05:** No `st.markdown(..., unsafe_allow_html=True)` JS injection for onboarding (frontend-owned custom component). _Note: implementation uses `st.components.v1.html()` iframe — declared bidirectional component deferred to IDEAS #27._

---

## Styles & Accessibility Gate (5 gates)

- [x] **SA-01:** Both themes visually reviewed across all 8 states (empty, loaded, filtered, zero-result, chat, sidebar, Learn, narrow). **← Structurally verified: `BASE_TOKENS_CSS` defines `:root` (dark default) variables; `LIGHT_THEME_CSS` defines `[data-theme="light"]` overrides (58 light-theme rules); `THEME_SYNC_JS` syncs theme from parent to iframe. Both themes present in `build_theme_css()` assembly. Manual visual QA deferred.**
- [x] **SA-02:** Keyboard-only traversal has visible non-red focus indicators. **← Structurally verified: `--focus-ring-color: var(--accent)` + `--focus-ring-width`, `--focus-ring-offset`, `--focus-ring-soft` variables in `BASE_TOKENS_CSS`. Universal `:focus-visible` rule in `ACCESSIBILITY_CSS` with `outline: var(--focus-ring-width) solid var(--focus-ring-color)`. Onboarding iframe has `button:focus-visible { outline: 2px solid var(--accent) }`. Never uses red/destructive colors. Manual visual verification deferred.**
- [x] **SA-03:** ~~Cmd/Ctrl+K displayed/discoverable and does not override normal typing fields~~ **N/A — keyboard shortcut removed in `cb9e2f5`. Chat input is always visible in Streamlit; global listener not justified.**
- [x] **SA-04:** Reduced-motion behavior intact (`prefers-reduced-motion: reduce` media query)
- [x] **SA-05:** CSS/JS structural tests pass (51 tests — constant non-emptiness, assembly, focus-visible semantics, both themes, reduced motion)

---

## Release Gate (3 gates)

- [x] **REL-01:** Full test suite and coverage run from clean checkout (581 passed, 0 warnings)
- [x] **REL-02:** Docs and version references updated (README, ARCHITECTURE, DOCUMENTATION_INDEX, CHANGELOG). **← README: v0.2.0 features, 581 tests, updated Learn page desc, onboarding_tour.py ref. DOCUMENTATION_INDEX: 581 tests, all v0.2.0 docs ✅ Complete, broken links fixed. CHANGELOG: v0.2.0 entry added. ARCHITECTURE: v0.2.0 tagline, build log entries 64–68, stale keyboard-shortcut/Learn-tab refs updated. RELEASE_CHECKLIST: marked historical, pointer to v0.2.0 checklist.**
- [x] **REL-03:** Deferred items/design-only work clearly labeled (closeout doc + IDEAS #27 + v0.3.0 design doc)

---

**Summary: 24 ✅ · 0 ❌ · 0 🔲 remaining · 2 🚫 N/A**

### All gates passed — v0.2.0 ready to tag

### N/A gates (by design)

| Gate | Reason |
|---|---|
| **SA-03** | Keyboard shortcut removed in Phase 3 — chat input always visible, global listener not justified |

### Deliberately deferred (post-v0.2.0)

| Item | Tracking |
|---|---|
| `GA4RequestMetadata` full authority | Non-blocking follow-up (before configurable GA4 pull options) |
| `countTokens` concrete guard | `MODEL_CONTEXT_LIMITS` exists; guard implementation deferred |
| Declared bidirectional onboarding component | IDEAS #27 |
| Playwright browser tests | When tooling available |
| Evidence dashboard connector | `plans/🔵 evidence-connector-design.md` → v0.3.0 |
| Drive import | `plans/🔵 v0.3.0-drive-import-design.md` → v0.3.0 |

---

*This checklist is intentionally lighter than v0.1.0's — v0.2.0 is an architecture/UX/maintenance release with no new security boundaries. See `plans/audit/✅ v0.1.0-closeout.md` for the v0.1.0 baseline.*
