.env
.env.*
!.env.example
venv/
.venv/
__pycache__/
*.pyc
.DS_Store
client_secrets.json
FREEBUFF_CONVERSATION*.md

# Cache & coverage
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Security
.streamlit/secrets.toml
oauth_state_*.json
# Real-Drive E2E: saved OAuth session (never commit live cookies)
tests/e2e/.auth/
# Google credential / token filenames (common defaults from sample code)
token.json
credentials.json
secrets.json
# Private key files
*.pem
*.key
id_rsa

# Build artifacts
docs/_build/

# Node / frontend
node_modules/
components/drive_picker_component_frontend/build/

# Artifact paths (PR 0 — prevent recurrence of committed data)
email/
*.eml
data/
uploads/
exports/
