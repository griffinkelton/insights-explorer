# UI Source Capture Manifest — `insights-whisperer-30` @ `8b4b7b9`

**Frozen source SHA:** `8b4b7b9cd8ee3ca5aa4b90c1c209d6e4e8b33cca` ("Added evidence and GA4 panels", 2026-08-06) — includes all 17 new commits; supersedes the stale `a71c371` capture.
**Captured:** 2026-08-06 · **Files:** 94 · **Total size:** ~568 KB · **Credential guard:** clean (verified).

This capture is **reference-only** (per the doc-role split) — it is the frozen source for the Phase 4 port. Any port must be made from this capture, not a live clone, so future Lovable changes cannot shift the reference mid-work.

## Exclusions (deliberately not captured)

- `.env` — **tracked in the source repo** (rotation is migration gate 1; see `migration/env-rotation-checklist.md`)
- Lockfiles (`bun.lock` / `package-lock.json`) — not needed for dependency reproduction at capture time
- Lovable gateway credentials/configuration (`connector-gateway.lovable.dev` key plumbing is inside captured files but is **do-not-port**)
- Generated route trees — captured (`routeTree.gen.ts`) as **reference only**; regenerated by the router plugin

## Port classifications

### Port/adapt (production UI — Phase 4 copy + FastAPI wiring)

| File | Purpose |
|---|---|
| `src/components/explorer/AiSummary.tsx` | AI summary card |
| `src/components/explorer/ChartCard.tsx` | Chart card wrapper |
| `src/components/explorer/ChartsRow.tsx` | Charts row layout |
| `src/components/explorer/Chat.tsx` | Chat panel (SSE reader → `/api/v1/chat`) |
| `src/components/explorer/DataPreview.tsx` | Data preview table |
| `src/components/explorer/DriveImportSheet.tsx` | Drive slide-out browse (Import must be wired to real `/api/v1/drive/download`) |
| `src/components/explorer/EmptyHero.tsx` | Empty state |
| `src/components/explorer/EquityPanel.tsx` | Equity insights panel |
| `src/components/explorer/ExportMenu.tsx` | Export menu (→ `/api/v1/export`) |
| `src/components/explorer/Markdown.tsx` | Markdown renderer |
| `src/components/explorer/OnboardingTour.tsx` | Onboarding tour |
| `src/components/explorer/ResearchPanel.tsx` | Research/sources panel |
| `src/components/explorer/Scorecard.tsx` | Metrics scorecard |
| `src/components/explorer/Sidebar.tsx` | Sidebar (Drive entry + nav) |
| `src/components/explorer/TopBar.tsx` | Top bar |
| `src/components/explorer/UploadZone.tsx` | Upload dropzone (→ `/api/v1/upload`) |
| `src/lib/explorer-store.tsx` | Context provider — **F3 wiring target** |
| `src/lib/utils.ts` | cn() helper |
| `src/router.tsx` | TanStack Router config |
| `src/styles.css` | Global styles |
| `src/routes/index.tsx` | Main route |
| `src/routes/learn.tsx` | Learn page |
| `src/routes/__root.tsx` | Root layout |
| `src/hooks/use-mobile.tsx` | Responsive hook |
| `package.json` | Dependency pins (ai@^7.0.48, react ^19.2, etc.) |
| `vite.config.ts` | Vite config (→ `@vitejs/plugin-react` + router plugin) |
| `tsconfig.json` | TS config |
| `components.json` | shadcn config |

### Reference only (version-pin / semantic reference)

| File | Purpose |
|---|---|
| `src/components/ui/*` (46 files) | Standard shadcn primitives — re-addable via `npx shadcn add`; captured to pin versions |
| `src/lib/measurement-contract.ts` | **Verified-faithful transcription** of `plans/ga4-measurement-contract.md` (5/5 rows); TS types generated from the canonical source, never from this file |
| `src/routeTree.gen.ts` | Generated — regenerate, never port manually |
| `src/routes/api/chat.ts` | Nitro route — **do-not-port** (replaced by `/api/v1/chat`); reference for wire-format behavior (`toTextStreamResponse`) |
| `src/routes/api/drive-files.ts` | Nitro route — **do-not-port** (replaced by `/api/v1/drive/list`) |
| `src/routes/api/research.ts` | Nitro route — **do-not-port** (evidence workstream deferred) |
| `src/lib/ai-gateway.server.ts` | Lovable AI gateway — **do-not-port** (AI routing stays in FastAPI) |
| `src/lib/research/sources.server.ts` | Source registry — **do-not-port as-is**; prototype mocks + Nitro; evidence source never enters the production registry |
| `src/lib/research/types.ts` | Research types — port/adapt only if evidence workstream starts |
| `src/lib/research/drive-browse.server.ts` | Drive browse adapter — **do-not-port** (Nitro/gateway); FastAPI reimplements `files.list` |

### Fixture only (mock data → `frontend/src/test/fixtures/`)

| File | Purpose |
|---|---|
| `src/lib/mock-ga4.ts` | GA4 mock — test fixture only, never runtime |
| `src/lib/mock-braintree.ts` | Braintree mock — test fixture only, never runtime |
| `src/lib/evidence/mock-evidence.ts` | Evidence mock — test fixture only (quarantine rule) |
| `src/lib/insights/engine.ts` | Deterministic insight engine — prototype of the deferred GA4-insights workstream; fixture/design reference only (gate 8) |

### Do not port (in `src/routes/api/` and above)

All Nitro/Lovable server routes (`api/chat.ts`, `api/drive-files.ts`, `api/research.ts`), `ai-gateway.server.ts`, `drive-browse.server.ts`, `sources.server.ts` — replaced by FastAPI `/api/v1/*` routes backed by `utils/`.

**Panels kept out of the first production slice (gate 8):** `EvidenceConnectorPanel.tsx`, `InsightCandidates.tsx`, `MeasurementContractPanel.tsx` (captured as prototypes; may live under `frontend/src/prototype/` with a "Demo / mock data" label if previewed).

---

*Source: `git show 8b4b7b9:<path>` from `griffinkelton/insights-whisperer-30`. Compiled 2026-08-06. Cross-refs: `LOVABLE-UPDATES-080525.md`, `LOVABLE-ACTIONS-080526.txt`, `migration/master-plan.md` §12.*
